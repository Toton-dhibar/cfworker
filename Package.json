{
  "name": "vless-worker",
  "version": "1.0.0",
  "description": "VLESS/VMess VPN on Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20231025.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
